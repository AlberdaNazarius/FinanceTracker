export type LocationBalance = {
  location_id: string;
  name: string;
  icon: string;
  color: string;
  archived: boolean;
  sort_order: number;
  is_default: boolean;
  currency_id: number;
  currency_code: string;
  balance: number;
}
